"""Pydantic DTOs for /v1 request/response shapes.

These are app-layer payloads distinct from the canonical document types
in ``app.schemas.generated`` (which model catalog entries, overlays, and
deployment records).
"""
